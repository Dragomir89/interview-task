const BASE_URL = "https://jsonplaceholder.typicode.com";

export enum appRouts {
  pageOne = "/",
  pageTwo = "/page-two",
}

export enum userURLS {
  user01 = `${BASE_URL}/users/1`,
  user02 = `${BASE_URL}/users/2`,
  user03 = `${BASE_URL}/users/3`,
}
